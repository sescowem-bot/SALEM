"use client";
import { useActionState, useState } from "react";
import { useFormStatus } from "react-dom";
import { Plus, Trash2 } from "lucide-react";
import { saveWebsiteDraftAction, type ActionState } from "./actions";
import type { FaqContent, PackagesContent, BookingContent, ResultsContent } from "@/lib/data/websiteContentTypes";
import { defaultFaqContent, defaultPackagesContent, defaultBookingContent, defaultResultsContent } from "@/components/salem/pages";
const input="w-full rounded-lg border border-border bg-secondary px-3 py-2.5 text-sm text-navy-deep";
function Save(){const {pending}=useFormStatus();return <button disabled={pending} className="rounded-full bg-navy px-6 py-3 text-sm font-semibold text-primary-foreground disabled:opacity-60">{pending?"Saving…":"Save draft"}</button>}
function TextField({label,value,onChange,area=false}:{label:string;value?:string;onChange:(v:string)=>void;area?:boolean}){return <label className="block text-sm font-medium text-navy-deep">{label}{area?<textarea rows={4} value={value||""} onChange={e=>onChange(e.target.value)} className={input+" mt-1.5 resize-y"}/>:<input value={value||""} onChange={e=>onChange(e.target.value)} className={input+" mt-1.5"}/>}</label>}
export function FaqEditorForm({content}:{content:FaqContent}){const [state,action]=useActionState(saveWebsiteDraftAction,{} as ActionState);const [form,setForm]=useState<FaqContent>({...defaultFaqContent,...content});const update=(fn:(x:FaqContent)=>FaqContent)=>setForm(fn);return <form action={action} className="space-y-5"><input type="hidden" name="pageKey" value="faq"/><input type="hidden" name="content" value={JSON.stringify(form)}/><div className="surface-card p-6 space-y-4"><TextField label="Page title" value={form.pageTitle} onChange={v=>update(f=>({...f,pageTitle:v}))}/><TextField label="Introduction" value={form.introduction} onChange={v=>update(f=>({...f,introduction:v}))} area/></div>{form.groups.map((g,gi)=><div key={g.id} className="surface-card p-6"><div className="flex items-center justify-between gap-3"><TextField label="Group heading" value={g.heading} onChange={v=>update(f=>({...f,groups:f.groups.map((x,i)=>i===gi?{...x,heading:v}:x)}))}/><button type="button" onClick={()=>update(f=>({...f,groups:f.groups.filter((_,i)=>i!==gi)}))} className="rounded-lg border border-border p-2 text-destructive"><Trash2 className="h-4 w-4"/></button></div><div className="mt-5 space-y-4">{g.items.map((item,ii)=><div key={item.id} className="rounded-xl border border-border bg-background p-4"><TextField label="Question" value={item.question} onChange={v=>update(f=>({...f,groups:f.groups.map((x,i)=>i!==gi?x:{...x,items:x.items.map((q,j)=>j===ii?{...q,question:v}:q)})}))}/><div className="mt-3"><TextField label="Answer" value={item.answer} onChange={v=>update(f=>({...f,groups:f.groups.map((x,i)=>i!==gi?x:{...x,items:x.items.map((q,j)=>j===ii?{...q,answer:v}:q)})}))} area/></div><button type="button" onClick={()=>update(f=>({...f,groups:f.groups.map((x,i)=>i!==gi?x:{...x,items:x.items.filter((_,j)=>j!==ii)})}))} className="mt-3 text-xs font-semibold text-destructive">Remove question</button></div>)}<button type="button" onClick={()=>update(f=>({...f,groups:f.groups.map((x,i)=>i!==gi?x:{...x,items:[...x.items,{id:crypto.randomUUID(),question:"",answer:""}]})}))} className="inline-flex items-center gap-2 text-sm font-semibold text-purple"><Plus className="h-4 w-4"/> Add question</button></div></div>)}<button type="button" onClick={()=>update(f=>({...f,groups:[...f.groups,{id:crypto.randomUUID(),heading:"New category",items:[]}]}))} className="inline-flex items-center gap-2 rounded-full border border-border px-4 py-2 text-sm font-semibold text-navy"><Plus className="h-4 w-4"/> Add FAQ category</button>{state.error?<p className="text-sm text-destructive">{state.error}</p>:null}<Save/></form>}
export function PackagesEditorForm({content}:{content:PackagesContent}){const [state,action]=useActionState(saveWebsiteDraftAction,{} as ActionState);const [form,setForm]=useState<PackagesContent>({...defaultPackagesContent,...content});const patch=(fn:(f:PackagesContent)=>PackagesContent)=>setForm(fn);return <form action={action} className="space-y-5"><input type="hidden" name="pageKey" value="packages"/><input type="hidden" name="content" value={JSON.stringify(form)}/><div className="surface-card p-6 space-y-4"><TextField label="Page title" value={form.pageTitle} onChange={v=>patch(f=>({...f,pageTitle:v}))}/><TextField label="Introduction" value={form.introduction} onChange={v=>patch(f=>({...f,introduction:v}))} area/><TextField label="Pricing note" value={form.pricingNote} onChange={v=>patch(f=>({...f,pricingNote:v}))}/></div>{form.packages.map((p,i)=><div key={p.id} className="surface-card p-6 space-y-4"><div className="flex justify-between"><h2 className="font-semibold text-navy-deep">Package {i+1}</h2><button type="button" onClick={()=>patch(f=>({...f,packages:f.packages.filter((_,j)=>j!==i)}))} className="text-destructive"><Trash2 className="h-4 w-4"/></button></div><TextField label="Package name" value={p.name} onChange={v=>patch(f=>({...f,packages:f.packages.map((x,j)=>j===i?{...x,name:v}:x)}))}/><TextField label="Description" value={p.description} onChange={v=>patch(f=>({...f,packages:f.packages.map((x,j)=>j===i?{...x,description:v}:x)}))} area/><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={!!p.featured} onChange={e=>patch(f=>({...f,packages:f.packages.map((x,j)=>j===i?{...x,featured:e.target.checked}:x)}))}/> Mark as featured</label><div><p className="text-sm font-medium text-navy-deep">Included tests / items</p><div className="mt-2 space-y-2">{p.tests.map((t,j)=><div key={j} className="flex gap-2"><input value={t} onChange={e=>patch(f=>({...f,packages:f.packages.map((x,k)=>k===i?{...x,tests:x.tests.map((v,n)=>n===j?e.target.value:v)}:x)}))} className={input}/><button type="button" onClick={()=>patch(f=>({...f,packages:f.packages.map((x,k)=>k===i?{...x,tests:x.tests.filter((_,n)=>n!==j)}:x)}))} className="text-destructive"><Trash2 className="h-4 w-4"/></button></div>)}</div><button type="button" onClick={()=>patch(f=>({...f,packages:f.packages.map((x,k)=>k===i?{...x,tests:[...x.tests,""]}:x)}))} className="mt-2 text-xs font-semibold text-purple">+ Add item</button></div><TextField label="Button label" value={p.ctaLabel} onChange={v=>patch(f=>({...f,packages:f.packages.map((x,j)=>j===i?{...x,ctaLabel:v}:x)}))}/></div>)}<button type="button" onClick={()=>patch(f=>({...f,packages:[...f.packages,{id:crypto.randomUUID(),name:"New package",description:"",tests:[]}]}))} className="inline-flex items-center gap-2 rounded-full border border-border px-4 py-2 text-sm font-semibold text-navy"><Plus className="h-4 w-4"/> Add package</button>{state.error?<p className="text-sm text-destructive">{state.error}</p>:null}<Save/></form>}
export function SimpleContentEditor({pageKey,content,type}:{pageKey:"booking"|"results";content:BookingContent|ResultsContent;type:"booking"|"results"}){const [state,action]=useActionState(saveWebsiteDraftAction,{} as ActionState);const initial=type==="booking"?{...defaultBookingContent,...content}:{...defaultResultsContent,...content};const [form,setForm]=useState<any>(initial);const fields=type==="booking"?["pageTitle","introduction","bookingNotice","confirmationTitle","confirmationMessage"]:["pageTitle","introduction","accessInstructions","helpMessage"];const labels:Record<string,string>={pageTitle:"Page title",introduction:"Introduction",bookingNotice:"Booking notice",confirmationTitle:"Confirmation title",confirmationMessage:"Confirmation message",accessInstructions:"Access instructions",helpMessage:"Help message"};return <form action={action} className="surface-card p-6 space-y-4"><input type="hidden" name="pageKey" value={pageKey}/><input type="hidden" name="content" value={JSON.stringify(form)}/>{fields.map(k=><TextField key={k} label={labels[k]} value={form[k]} onChange={v=>setForm((f:any)=>({...f,[k]:v}))} area={k!=="pageTitle"}/>) }{state.error?<p className="text-sm text-destructive">{state.error}</p>:null}<Save/></form>}
